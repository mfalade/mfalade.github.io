import styled from 'styled-components';

export const HamburgerContainer = styled.button`
  position: relative;
  background: none;
  outline: none;
  border: none;
  cursor: pointer;
  display: inline-flex;
  z-index: 10;
`;
