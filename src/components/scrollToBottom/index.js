import React from 'react';

import { IconContainer, StyledIcon } from './styles';

function ScrollToBottom() {
  return (
    <IconContainer>
      <StyledIcon animationDelay={1} />
      <StyledIcon animationDelay={2} />
    </IconContainer>
  );
}

export default ScrollToBottom;
